#include "GroundObj.h"

GroundObj::GroundObj(Vector2f p_pos, Vector2f p_scale, std::shared_ptr<Image> p_tex)
    : GameObject(p_pos, p_scale, p_tex)
{
    // any additional initialization for the ground object
    // can be done here
}
